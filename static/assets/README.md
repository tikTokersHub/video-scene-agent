# Static Assets

Use this directory for small, intentional frontend assets only.

Suggested structure:

```text
assets/
  images/
    demo-frame-001.jpg
    demo-frame-002.jpg
```

Evidence frames are served dynamically from prepared videos, so generated
uploads and extracted thumbnails should stay out of Git.
